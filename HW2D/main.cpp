//Нахождение площади пересекающихся треугольников

//1. Реализация класса (структуру) точки
//2. Отрисовака треугольника в консоли

#include "header.h"

//20.12.2023 My variat 
//struct Point
//{
//	double x;
//	double y;
//	Point(double x = 0., double y = 0.) : x(x), y(y) {};
//};

//std::istream& operator>>(std::istream& stream, Point& point)
//{
//	stream >> point.x;
//	stream >> point.y;
//	return stream;
//}


int main(int argc, char* argv[])
{
	//std::cout << argv[0];

	//Point tr1_p1(1, 1), tr1_p2(4, 1), tr1_p3(1, 4);

	/*Point* tr1 = new Point[3];

	tr1[0] = Point(1, 1);
	tr1[1] = Point(4, 1);
	tr1[2] = Point(1, 4);

	char point = '*';
	char space = ' ';

	char tmp_drawer = space;

	int size_x = 9;
	int size_y = 9;

	for (int y_index = size_y; y_index > 0; y_index--)
	{
		std::cout << y_index;
		for(int x_index = 1; x_index < size_x; x_index++)
		{
			tmp_drawer = space;
			for (int tr1_index = 0; tr1_index < 3; tr1_index++)
			{
				if (tr1[tr1_index].x == x_index && tr1[tr1_index].y == y_index)
				{
					tmp_drawer = point;
					break;
				}
			}
			std::cout << " " << tmp_drawer;
		}
		std::cout << "\n";
	}

	for (int index = 0; index < size_x + 1; index++)
	{
		std::cout << index << " ";
	}

	delete []tr1;*/

	//float a = NAN;
	//std::cout << "NAN != NAN: " << (NAN != NAN) << "\n";
	//std::cout << "NAN == NAN: " << (NAN == NAN) << "\n";
	//std::cout << "inf != inf: " << (INFINITY != INFINITY) << "\n";
	//std::cout << "inf == inf: " << (INFINITY == INFINITY) << "\n";
	//std::cout << "inf != -inf: " << (INFINITY != -INFINITY) << "\n";
	//std::cout << "inf == -inf: " << (INFINITY == -INFINITY) << "\n";

}

